{{template "header"}}

<h5>Evaluación</h5>
<div class="row" style="margin-bottom: 3rem;">
    <h2 class="text-muted">
        {{safe .Title}} {{ .ID }}
    </h2>
    <div class="col-md-8 mt-5 offset-md-1">
        {{ markdown .Content }}
    </div>
</div>

{{template "interpreter" .}}

{{template "footer"}}